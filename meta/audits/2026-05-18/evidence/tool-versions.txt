# Tool versions captured at preflight + amended after Node install
# Audit: 2026-05-18 brownfield docs audit
# Initial recording: 2026-05-18 (preflight, no Node)
# Amendment:         2026-05-18 (after `nvm install` and `npm ci`)

## AVAILABLE — INITIAL PREFLIGHT
ripgrep   14.1.1 (rev d29891451d)
git       2.50.1 (Apple Git-155)
python3   3.9.6 (stdlib only; no PyYAML)
jq        1.7.1-apple
awk       BSD awk (macOS /usr/bin/awk)
sed       BSD sed
find      bfs (claude-code shim over BSD find)
wc        BSD wc

## NEWLY AVAILABLE — AMENDMENT PASS
node      24.15.0     (via nvm)
npm       11.12.1
nvm       installed at ~/.nvm; v24.15.0 set as default

## TOOLS RUN VIA NPX IN THE AMENDMENT PASS
gray-matter           4.x (via npm install --no-save)
remark + remark-mdx   latest (via npm install --no-save)
unist-util-visit      latest (via npm install --no-save)
markdown-link-check   3.14.2 (used briefly; switched to Node fetch for cleaner result)
markdownlint-cli2     v0.22.1 (markdownlint v0.40.0)
alex                  latest (npx)
shellcheck            v0.11.0 (via npm shellcheck wrapper; did not detect F-0001)
pa11y                 latest (npx, ran against local dist/)
afdocs                latest (npx; ran against live https://promptless.ai)

## RAN BUT WITH CAVEATS
External link checker
  - Used Node 24's built-in fetch over the 28 unique external URLs
  - lychee not installable via npx (Rust binary); tooling-spec keeps lychee as the long-term pick

## STILL UNAVAILABLE / DEFERRED
vale              not run (Phase 4 Expanded; not selected this round)
cspell            not run (Phase 4 Expanded; not selected this round)
lighthouse-ci     not run (Phase 5 Expanded; not selected this round)
gh CLI            still not installed; AFDocs was run directly via `npx afdocs` (no scheduled-workflow integration)
Doc Detective full run (with auth secrets) — not run; product test of login flow needs credentials

## SHELL ENV
PATH segments include /opt/homebrew/bin, /usr/local/bin, /usr/bin
Node is loaded by sourcing $HOME/.nvm/nvm.sh in each shell invocation
