---
title: Frequently asked questions
description: Short answers to the common questions about governing agent instructions with Promptless, each linking to the page that covers it in full.
slug: docs/governance/reference/faq
type: reference
tags:
  - agent-instructions
  - governance
  - reference
timestamp: "2026-08-20T00:00:00Z"
sidebar:
  hidden: false
  order: 4
---

Answers to common questions about Promptless for Agent Instructions. Each answer links to the page that covers the topic in full.

## Common questions

### What leaves my host?

Your host uploads verbatim native trace bytes and batch metadata only to the worker you run in your own infrastructure. Hosted Promptless receives a limited metadata projection (identifiers, counts, and status), never trace content. See [Trust and data model](/docs/governance/start-here/trust-and-data-model).

### Which agents does Promptless support?

Promptless compiles plugins for four targets (Claude, Codex, Cursor, and Gemini) and ingests session traces from three sources (Claude Code, Claude Desktop, and Codex). See [Supported agents](/docs/governance/reference/supported-agents).

### What is the gate before a fix merges?

Human review. A remediation agent opens a pull request against your Instruction Hub, and a person reviews and merges it. There is no automated evaluation gate. See [Remediate findings](/docs/governance/findings-and-remediation/remediate-findings).

### Where does the pig toolchain live?

The `pig` toolchain is the public Python package `promptless-instruction-hub`, in the `Promptless/instruction-hub-toolchain` repository. It runs in your continuous-integration pipeline, not as a service. See [Set up your Instruction Hub](/docs/governance/get-started/set-up-your-instruction-hub).

### What does the worker need to run?

A Postgres database and an S3 bucket you own, plus a `plih_` install token. Before the worker can produce findings, you also configure a model provider through the `INSTRUCTION_HUB_ANALYSIS_MODEL_*` variables, since the Friction Analyzer sends its analysis input there. You deploy the worker into your own Kubernetes cluster with Helm. See [Deploy the analyzer worker](/docs/governance/deploy-the-worker/deploy-the-analyzer-worker) and the [Configuration reference](/docs/governance/deploy-the-worker/configuration-reference).

### Does hosted Promptless see my GitHub credentials?

No. The worker never receives GitHub credentials. Findings are projected to GitHub issues by hosted Promptless, and remediation runs with a repository-scoped token in an isolated agent. See [Trust and data model](/docs/governance/start-here/trust-and-data-model).

## Get support

Reach our support team at [help@gopromptless.ai](mailto\:help@gopromptless.ai) if you do not see your question answered here. During onboarding, we also set up a shared Slack Connect channel between your team and ours for direct support.
