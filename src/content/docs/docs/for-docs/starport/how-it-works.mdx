---
title: How Starport works
description: "Understand the managed model: Promptless stands up the site, runs hosting and builds, and keeps content current while your content lives in a Git repository you own."
slug: docs/for-docs/starport/how-it-works
type: guide
tags:
  - starport
  - guide
timestamp: "2026-09-11T00:00:00Z"
sidebar:
  hidden: false
  order: 71
  label: How it works
---

import { Aside, Steps } from '@astrojs/starlight/components';

Starport's managed model decides who runs what and what your team owns. For the configuration you set yourself, see [Configure your Starport site](/docs/for-docs/starport/configuring-starport). For the switch onto Starport, see [Migrate to Starport](/docs/for-docs/starport/migration).

Starport is a docs-as-code stack that Promptless operates on your behalf. In the docs-as-code model, your pages live as source files in a Git repository your team owns, versioned and reviewed like code. Promptless runs everything around it: the site framework, the build pipeline, hosting, and the agent that keeps content current.

## The managed model end to end

Promptless owns the operational pieces of your documentation site, so your team writes and reviews content while Promptless maintains the infrastructure.

<Steps>

1. Promptless stands up your site on the Starport template, built on Astro Starlight.

2. Promptless runs your builds and deploys, so a merged change publishes through the hosting provider without manual steps.

3. Promptless hosts the published site and keeps its supporting features current, including search, redirects, and the agent-ready surfaces. The site runs on Promptless-managed hosting or on your own hosting account.

4. The Promptless agent detects when your product changes and opens documentation pull requests against your repository for your team to review.

</Steps>

<Aside type="note">
  Promptless proposes every content change as a reviewable pull request. Your team approves and merges each one, so you stay in control of what publishes.
</Aside>

## What you own

Your content lives in a Git repository your team owns and controls. Starport follows the docs-as-code model: your pages are source files in that repository. Promptless proposes edits the way a teammate would, by branching, changing files, and opening a pull request you review.

Because the repository is yours, you keep full history, access control, and the ability to edit content directly at any time. For the fundamentals of the docs-as-code model and why Promptless builds on it, see [Understand docs-as-code](/docs/for-docs/migrate/why-docs-as-code).

After onboarding, you tune the site yourself through its configuration files. See [Configure your Starport site](/docs/for-docs/starport/configuring-starport) for the options you control, and [Keep your Starport docs current](/docs/for-docs/starport/keep-docs-current) for how the agent maintains content over time.

## Prerequisites

To run on Starport, your documentation needs a docs-as-code footing:

- A Git repository your team owns, which holds your content as source files.
- Content in a format Starport builds from, authored in Markdown or MDX.

If your docs still live in a hosted editor that stores content in a proprietary database, the move to docs-as-code comes first. [Migrate to Starport](/docs/for-docs/starport/migration) covers the managed switch, and Promptless handles that move for you.
