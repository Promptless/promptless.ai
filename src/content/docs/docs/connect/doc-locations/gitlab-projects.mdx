---
title: GitLab projects (docs as code)
slug: docs/connect/doc-locations/gitlab-projects
type: reference
tags:
  - connect
  - doc-locations
  - reference
timestamp: "2026-07-15T00:00:00Z"
sidebar:
  hidden: false
  order: 24
---

import { Aside } from '@astrojs/starlight/components';

You can store your documentation in a GitLab project and have Promptless publish updates there. This "docs as code" approach keeps your documentation under version control alongside the rest of your work, and it supports both gitlab.com and self-hosted GitLab.

<Aside type="note">
  **Prerequisite**: Before creating a GitLab-based doc collection, you must first [connect GitLab](/docs/reference/integrations/gitlab) on the integrations page. The connected group access token must have the write-capable `api` scope so Promptless can push a branch and open a merge request.
</Aside>

## How it works

1. **Project setup**: Your documentation files live in a GitLab project.
2. **Promptless access**: Promptless reaches the project using your connected GitLab group access token, which needs the `api` scope.
3. **Automatic merge requests**: When documentation updates are needed, Promptless pushes a branch and opens a merge request in your docs project.
4. **Platform sync**: Your documentation platform automatically syncs changes from GitLab.

## Configuration

You can configure your doc collections on the [Configuration page](https://app.gopromptless.ai/configuration). Doc collections are defined in the `doc_collections` section of your `promptless.yaml` file, keyed by the GitLab project path (`group/project`, including subgroups):

```yaml
doc_collections:
  acme/docs:                 # gitlab.com project
    platform: gitlab
    docs_framework: docusaurus
    docs_root_url: https://docs.acme.com
    filter:
      - docs/
  acme/handbook:             # self-hosted GitLab project
    platform: gitlab
    host: gitlab.acme.com
    docs_framework: mkdocs
```

Set `platform: gitlab` on each GitLab-backed collection. Leave `host` off for gitlab.com; for self-hosted GitLab, set it to your instance hostname (just the hostname—no `https://` and no path). See the [Configuration Reference](/docs/reference/configuration-reference#doc-collections) for the complete schema.

<Aside type="note">
  During onboarding, the project picker discovers gitlab.com projects automatically. To add a self-hosted GitLab project, create the collection in the configuration editor and enter your instance hostname.
</Aside>

## Automatic merge request creation

When automatic PR creation (`auto_create_pr`) is enabled in your [publishing policies](/docs/reference/configuration-reference#policies), Promptless opens a merge request in your documentation project with the suggested changes. Merge request status is tracked the same way GitHub pull request status is. Because GitLab has no draft flag at creation time, Promptless marks a draft merge request with a `Draft:` title prefix.

You can also set `auto_merge: true` in your publishing policies to have Promptless merge documentation merge requests automatically. See [Policies](/docs/reference/configuration-reference#policies) for how publishing rules resolve.

## Path scope (documentation directories)

Path scope controls which files Promptless can modify in your documentation project, exactly as it does for GitHub repositories. When you specify directories during doc collection setup, Promptless enforces these boundaries:

- **All files allowed (default):** If no directories are specified, Promptless can create and modify files anywhere in the project.
- **Scoped to specific paths:** When directories are specified, Promptless can only modify files that match one of those paths exactly or are nested inside one of those directories.

Path scopes support both directories and individual files. A scope entry like `CHANGELOG.md` allows only that specific file, while `docs/` allows any file under that directory. When Promptless creates a suggestion with changes outside the configured scope, the suggestion is rejected with an error listing which files are out of scope.

## Setup instructions

To connect GitLab to Promptless, see the [GitLab integration](/docs/reference/integrations/gitlab) setup guide.
