---
import Admonition from './_Admonition.astro';

export interface Props {
  title?: string;
}

const { title } = Astro.props;
const showTitle = typeof title === 'string' && title.trim().length > 0;
---

<Admonition kind="info" title={title} showTitle={showTitle}>
  <slot />
</Admonition>
