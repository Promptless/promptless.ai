---
export interface Props {
  cols?: number;
}

const { cols = 1 } = Astro.props;
const normalized = Number.isFinite(Number(cols)) ? Math.min(Math.max(Number(cols), 1), 3) : 1;
---

<div class:list={['fern-card-grid', `cols-${normalized}`]}>
  <slot />
</div>
