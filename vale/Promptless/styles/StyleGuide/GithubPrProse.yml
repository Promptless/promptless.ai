# Rung: Stated, conditional (warning)
# Source: $KB_REPO/style/client_style_guide.md, "Refer to GitHub PR triggers in plain language, not the `github_pr` identifier" (Terminology).
extends: existence
message: "In prose, write 'GitHub PR trigger' rather than the code identifier '%s'; reserve 'github_pr' for literal config/YAML values."
level: warning
tokens:
  - github_pr
